[package]
name = "idxlens_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
quick-xml = "0.36"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
zip = { version = "2.2", default-features = false, features = ["deflate"] }
