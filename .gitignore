target/
*.zip

# Kunci API — jangan pernah masuk repo (dibaca dari environment saja)
.env
.env.*
*.key

# Artefak jalur vision (Fase 1-6): PNG halaman, cache API, bukti per-emiten.
# Bukan kode; diregenerasi dari PDF sumber.
/out/
/cache/
*.png

# PDF/laporan sumber tidak di-commit (besar, dan sudah ada di ~/.idxlens/data)
*.pdf
